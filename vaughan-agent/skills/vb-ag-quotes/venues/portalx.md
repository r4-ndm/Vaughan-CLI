# PortalX — browserless only

## Identity

- **Venue id:** `portalx` / `PortalX`
- **Web swap UI:** none

## Quote path

Use MCP **`quote_swap`** when integration is live — not VB.

## MCP

`browser_open_agg` with `portalx` has no public UI target.
