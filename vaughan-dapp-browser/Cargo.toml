[package]
name = "vaughan-dapp-browser"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Optional allowlisted Chromium dApp shell for Vaughan (EIP-1193 → local provider)"
publish = false

[[bin]]
name = "vaughan-dapp-browser"
path = "src/main.rs"

[dependencies]
clap = { version = "4", features = ["derive"] }
dirs = { workspace = true }
serde_json = { workspace = true }
url = { workspace = true }
